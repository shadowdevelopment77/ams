# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright internal tooling output — not meant to be committed.
# Curated screenshots for human review live in e2e/screenshots/ instead,
# which is NOT ignored.
/playwright-report
/test-results

# Regenerated by e2e/reset-fixtures.sh on every run (dev-DB IDs tied to
# whatever the current fixture pass created) -- not meant to be committed.
/e2e/fixtures/data.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
